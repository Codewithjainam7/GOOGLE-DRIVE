# Claude 3 Opus - Comprehensive Mobile UI/UX Design Prompt

**Instructions for User:** 
Copy the entire text below this line and paste it into Claude 3 Opus. I have embedded the EXACT SOURCE CODE of your application inside this prompt so Opus has 100% full context to rewrite it flawlessly.

---
**[COPY EVERYTHING BELOW THIS LINE]**

**System Prompt & Role:**
You are an elite Principal UI/UX Designer and Frontend Architect specializing in React, Next.js, Framer Motion, and Tailwind CSS. 
I have a Next.js web application (an AI-Powered Google Drive Invoice Scanner Dashboard) that has an absolutely stunning, highly polished desktop (PC) layout. 

**My core requirement is this:** I need you to design and code the **mobile responsive layout** and UI/UX for this application. It MUST have the exact same colors, themes, and aesthetic vibe as the PC version so they look identical in branding—but the mobile version needs a vastly superior, optimized UI/UX layout tailored specifically for small touch screens.

### 1. The Aesthetic & Theme (MUST Match PC Version Exactly)
Do not invent new colors. You must strictly use the following "Liquid Glassmorphism" theme from the PC version:
- **Background Canvas:** Deep space navy (`#050a14`).
- **Glass Surfaces (Cards/Containers):** `bg-[#0c101a]/80 backdrop-blur-2xl border border-white/[0.08]`. 
- **Primary Accents:** Electric Purple (`#6C63FF` to `#857dff`). Use this for primary buttons, glowing borders, and active states.
- **Status Accents:** Emerald Green (`#00d68f`) for "Live/Connected" status dots and success messages.
- **Elevation & Depth:** Use heavy backdrop blurs (`backdrop-blur-2xl`), 1px translucent white borders (`border-white/[0.08]`), and soft box-shadows. No flat, boring panels.
- **Typography:** Modern, high-tech sans-serif. Use gradient text for important headers (`bg-gradient-to-r from-white via-slate-100 to-slate-400 bg-clip-text text-transparent`).

### 2. The Current Mobile Framework (Already in place)
- **Top Header:** A sticky glass navbar containing the title "Google Drive" and a pulsing green "Live" indicator.
- **Bottom Navigation:** A floating frosted glass "pill dock" fixed at the bottom (`fixed bottom-4 left-4 right-4`).

### 3. Your Task: Code the Mobile Layout
I have provided my exact source code below for `page.tsx` and `InvoiceScanner.tsx`.
I need you to output the complete, refactored React/Tailwind code for these two files, fixing the following mobile issues:

**Section A: The Metric Summary Grid (in `page.tsx` and `InvoiceScanner.tsx`)**
*Mobile UI/UX Instructions:* Design a highly optimized mobile arrangement. Instead of a cramped row, create a beautiful 2x2 grid (`grid-cols-2 gap-3`). Make the "Total Amount" card larger or full-width so it stands out like a premium fintech app. Keep the glassmorphism aesthetic intact.

**Section B: The AI Invoice Scanner Control Panel (in `InvoiceScanner.tsx`)**
This section contains a title ("Invoice & Expense AI Extractor") and three action buttons.
*Mobile UI/UX Instructions:* On PC, these sit side-by-side. On mobile, this looks terrible. I need you to design a mobile layout where these buttons are beautifully stacked vertically (`flex-col items-stretch w-full`). Add rich purple gradients to the primary scan button, hover/tap animations using Framer Motion, and glowing drop-shadows.

**Section C: The Scanned Invoices Data Display (in `InvoiceScanner.tsx`)**
Currently, scanned invoices are displayed in a standard HTML `<table>` (File Name, Amount, Extracted Text, Date). 
*Mobile UI/UX Instructions:* Data tables are awful on mobile. I need you to design a **Mobile-First "Card List" View**. Hide the `<table>` on mobile (`hidden md:table`), and replace it with a vertical stack of mini glass cards. Each mini card should represent one invoice, showing a document icon, the filename, the date, and the Amount prominently aligned to the right. 

### Final Constraints:
- Output only production-ready React code with Tailwind CSS.
- Ensure strict mobile responsiveness using Tailwind prefixes (e.g., `flex-col md:flex-row`).
- Prevent any horizontal scrolling (`min-w-0`, `overflow-x-hidden`).
- The mobile layout MUST look like a native iOS/Android premium app while retaining 100% of the PC version's dark mode glass theme.

---
### SOURCE CODE CONTEXT

**File 1: `src/app/page.tsx` (Dashboard Metrics to refactor into 2x2 grid on mobile)**
```tsx
              {/* Stats Grid */}
              <div className="grid grid-cols-2 lg:grid-cols-4 gap-3.5">
                {[
                  { label: "Access Mode", value: isConnected ? "Read Only" : "Disabled", dot: isConnected },
                  { label: "Folder Lock", value: folderLocked ? "Active" : isConnected ? "Pending" : "—", dot: folderLocked },
                  { label: "API Status", value: isConnected ? "Secure SSL" : "Offline", dot: isConnected },
                  { label: "Documents", value: folderLocked ? "Scoped" : "None", dot: folderLocked },
                ].map((stat) => (
                  <div key={stat.label} className="p-4 rounded-2xl glass-surface border border-white/[0.03] group hover:border-[#6C63FF]/30 transition-all duration-300">
                    <div className="flex items-center gap-1.5 mb-2">
                      <div className={`w-1.5 h-1.5 rounded-full ${stat.dot ? "bg-[#00d68f] shadow-[0_0_6px_#00d68f]" : "bg-[#4a5a72]"}`} />
                      <span className="text-[9px] font-bold text-[#7a8ba3] uppercase tracking-widest">{stat.label}</span>
                    </div>
                    <span className={`text-sm sm:text-base font-black tracking-tight ${stat.dot ? "text-[#00d68f]" : "text-white"}`}>{stat.value}</span>
                  </div>
                ))}
              </div>
```

**File 2: `src/components/InvoiceScanner.tsx` (Target sections to refactor)**

**Target 1: Action Buttons**
```tsx
          {/* Action buttons */}
          <div className="flex flex-col sm:flex-row flex-wrap items-stretch sm:items-center gap-3 w-full lg:w-auto">
            <motion.button
              whileHover={{ scale: 1.02 }}
              whileTap={{ scale: 0.98 }}
              onClick={scanInvoices}
              disabled={phase === "scanning"}
              className="px-5 py-3 rounded-xl text-[12px] font-black tracking-wide uppercase transition-all bg-[#6C63FF] hover:bg-[#5A52E0] text-white disabled:opacity-50 flex items-center justify-center gap-2 flex-1 sm:flex-none shadow-lg shadow-[#6C63FF]/20 border border-white/[0.08]"
            >
              {phase === "scanning" ? <Loader2 className="w-4 h-4 animate-spin text-white" /> : <Scan className="w-4 h-4 text-white" />}
              {phase === "scanning" ? "Scanning Drive..." : invoices.length > 0 ? "Scan & Refresh" : "Scan Invoices"}
            </motion.button>
            {invoices.length > 0 && (
              <>
                <motion.button onClick={exportToSheets}> {/* Export Button */} </motion.button>
                <motion.button onClick={clearHistory}> Reset History </motion.button>
              </>
            )}
          </div>
```

**Target 2: Table to Card List**
```tsx
            {/* Premium structured list table (prevents layout wrapping / overlapping entirely) */}
            <div className="overflow-x-auto">
              <table className="w-full min-w-[900px] table-fixed hidden md:table">
                 {/* Desktop Table Header & Body */}
              </table>
            </div>

            {/* TODO FOR OPUS: Add Mobile Card List View Here (visible only on mobile: block md:hidden) */}
```
